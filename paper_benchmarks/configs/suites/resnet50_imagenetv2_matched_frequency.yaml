suite_id: resnet50_imagenetv2_matched_frequency
benchmark_mode: e2e_model
description: ResNet-50 ImageNetV2 matched-frequency external benchmark.
workload_type: dataset_records
workload_path: /home/gb10/Projects/Kernal-Forge/datacollection/external_datasets/imagenetv2/imagenetv2_matched_frequency_manifest.jsonl
workload_slug: imagenetv2_matched_frequency_resnet50_manifest_v1
dataset_license: ImageNetV2 terms
dataset_access_terms: Public ImageNetV2 matched-frequency benchmark; do not redistribute images in artifacts.
synthetic_workload: false
variants:
  - eager
  - torch_compile
  - kf_cast
stages:
  - load
  - compile
  - warmup
  - total_generate
batch_sizes:
  - 1
  - 32
shape_or_length_buckets:
  - bucket_id: image_224
    image_size: 224
warmup_count: 20
timed_run_count: 100
generation_mode: greedy
cache_mode: kv_cache_off
include_tokenization_in_timing: false
measure_prefill_decode_separately: false
device: cuda
notes:
  - Stage total_generate represents steady-state image inference for compatibility with the existing model-level artifact schema.
  - Use --full-workload for a one-pass full ImageNetV2 matched-frequency run.
